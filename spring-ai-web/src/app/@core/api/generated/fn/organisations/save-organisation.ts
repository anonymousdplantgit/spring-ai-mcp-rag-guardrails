/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrganizationResource } from '../../models/organization-resource';

export interface SaveOrganisation$Params {
      body: OrganizationResource
}

export function saveOrganisation(http: HttpClient, rootUrl: string, params: SaveOrganisation$Params, context?: HttpContext): Observable<StrictHttpResponse<OrganizationResource>> {
  const rb = new RequestBuilder(rootUrl, saveOrganisation.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OrganizationResource>;
    })
  );
}

saveOrganisation.PATH = '/api/organisations';
