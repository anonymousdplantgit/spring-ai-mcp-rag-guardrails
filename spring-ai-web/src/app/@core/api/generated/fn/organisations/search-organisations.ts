/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrganizationResource } from '../../models/organization-resource';

export interface SearchOrganisations$Params {

/**
 * Search query for organisation autocomplete
 */
  query: string;
}

export function searchOrganisations(http: HttpClient, rootUrl: string, params: SearchOrganisations$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<OrganizationResource>>> {
  const rb = new RequestBuilder(rootUrl, searchOrganisations.PATH, 'get');
  if (params) {
    rb.query('query', params.query, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<OrganizationResource>>;
    })
  );
}

searchOrganisations.PATH = '/api/organisations/search';
