/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TrainingDataResource } from '../../models/training-data-resource';

export interface SearchTrainingData$Params {

/**
 * Search query for training data autocomplete
 */
  query: string;
}

export function searchTrainingData(http: HttpClient, rootUrl: string, params: SearchTrainingData$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TrainingDataResource>>> {
  const rb = new RequestBuilder(rootUrl, searchTrainingData.PATH, 'get');
  if (params) {
    rb.query('query', params.query, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TrainingDataResource>>;
    })
  );
}

searchTrainingData.PATH = '/api/training-data/search';
