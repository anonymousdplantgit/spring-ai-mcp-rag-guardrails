/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TrainingDataResource } from '../../models/training-data-resource';

export interface GetTrainingData1$Params {

/**
 * UUID of the training data
 */
  id: string;
}

export function getTrainingData1(http: HttpClient, rootUrl: string, params: GetTrainingData1$Params, context?: HttpContext): Observable<StrictHttpResponse<TrainingDataResource>> {
  const rb = new RequestBuilder(rootUrl, getTrainingData1.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TrainingDataResource>;
    })
  );
}

getTrainingData1.PATH = '/api/training-data/{id}';
