/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TrainingDataResource } from '../../models/training-data-resource';

export interface SaveTrainingData$Params {
      body: TrainingDataResource
}

export function saveTrainingData(http: HttpClient, rootUrl: string, params: SaveTrainingData$Params, context?: HttpContext): Observable<StrictHttpResponse<TrainingDataResource>> {
  const rb = new RequestBuilder(rootUrl, saveTrainingData.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TrainingDataResource>;
    })
  );
}

saveTrainingData.PATH = '/api/training-data';
