/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BotStatistics } from '../../models/bot-statistics';

export interface GetBotStatistics$Params {

/**
 * Bot ID
 */
  botId: string;
}

export function getBotStatistics(http: HttpClient, rootUrl: string, params: GetBotStatistics$Params, context?: HttpContext): Observable<StrictHttpResponse<BotStatistics>> {
  const rb = new RequestBuilder(rootUrl, getBotStatistics.PATH, 'get');
  if (params) {
    rb.path('botId', params.botId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BotStatistics>;
    })
  );
}

getBotStatistics.PATH = '/api/statistics/bot/{botId}';
