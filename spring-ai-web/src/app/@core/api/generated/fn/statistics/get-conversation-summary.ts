/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ConversationSummaryReport } from '../../models/conversation-summary-report';

export interface GetConversationSummary$Params {

/**
 * Bot ID
 */
  botId: string;

/**
 * Start date
 */
  startDate: string;

/**
 * End date
 */
  endDate: string;
}

export function getConversationSummary(http: HttpClient, rootUrl: string, params: GetConversationSummary$Params, context?: HttpContext): Observable<StrictHttpResponse<ConversationSummaryReport>> {
  const rb = new RequestBuilder(rootUrl, getConversationSummary.PATH, 'get');
  if (params) {
    rb.path('botId', params.botId, {});
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ConversationSummaryReport>;
    })
  );
}

getConversationSummary.PATH = '/api/statistics/bot/{botId}/summary';
