/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ConversationResource } from '../../models/conversation-resource';

export interface SearchConversations$Params {

/**
 * Search query for conversation autocomplete
 */
  query: string;
}

export function searchConversations(http: HttpClient, rootUrl: string, params: SearchConversations$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ConversationResource>>> {
  const rb = new RequestBuilder(rootUrl, searchConversations.PATH, 'get');
  if (params) {
    rb.query('query', params.query, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ConversationResource>>;
    })
  );
}

searchConversations.PATH = '/api/conversations/search';
