/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageMessageResource } from '../../models/page-message-resource';

export interface GetConversationMessagesPage$Params {

/**
 * UUID of the conversation
 */
  conversationId: string;

/**
 * Page number
 */
  page?: number;

/**
 * Page size
 */
  size?: number;
}

export function getConversationMessagesPage(http: HttpClient, rootUrl: string, params: GetConversationMessagesPage$Params, context?: HttpContext): Observable<StrictHttpResponse<PageMessageResource>> {
  const rb = new RequestBuilder(rootUrl, getConversationMessagesPage.PATH, 'get');
  if (params) {
    rb.path('conversationId', params.conversationId, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageMessageResource>;
    })
  );
}

getConversationMessagesPage.PATH = '/api/conversations/{conversationId}/messages/paged';
