/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ConversationSummary } from '../../models/conversation-summary';

export interface GetConversationSummary1$Params {

/**
 * conversation ID
 */
  conversationId: string;
}

export function getConversationSummary1(http: HttpClient, rootUrl: string, params: GetConversationSummary1$Params, context?: HttpContext): Observable<StrictHttpResponse<ConversationSummary>> {
  const rb = new RequestBuilder(rootUrl, getConversationSummary1.PATH, 'get');
  if (params) {
    rb.path('conversationId', params.conversationId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ConversationSummary>;
    })
  );
}

getConversationSummary1.PATH = '/api/conversations/external/{conversationId}/summary';
