/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MessageResource } from '../../models/message-resource';

export interface SearchMessages$Params {

/**
 * Search text
 */
  query: string;

/**
 * Conversation ID to search within
 */
  conversationId?: string;

/**
 * Page number
 */
  page?: number;

/**
 * Page size
 */
  size?: number;
}

export function searchMessages(http: HttpClient, rootUrl: string, params: SearchMessages$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<MessageResource>>> {
  const rb = new RequestBuilder(rootUrl, searchMessages.PATH, 'get');
  if (params) {
    rb.query('query', params.query, {});
    rb.query('conversationId', params.conversationId, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<MessageResource>>;
    })
  );
}

searchMessages.PATH = '/api/conversations/search/messages';
