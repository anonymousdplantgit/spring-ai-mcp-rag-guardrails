/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ConversationResource } from '../../models/conversation-resource';

export interface GetConversationsByDateRange$Params {

/**
 * Start date
 */
  startDate: string;

/**
 * End date
 */
  endDate: string;

/**
 * Bot ID filter
 */
  botId?: string;
}

export function getConversationsByDateRange(http: HttpClient, rootUrl: string, params: GetConversationsByDateRange$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ConversationResource>>> {
  const rb = new RequestBuilder(rootUrl, getConversationsByDateRange.PATH, 'get');
  if (params) {
    rb.query('startDate', params.startDate, {});
    rb.query('endDate', params.endDate, {});
    rb.query('botId', params.botId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ConversationResource>>;
    })
  );
}

getConversationsByDateRange.PATH = '/api/conversations/range';
