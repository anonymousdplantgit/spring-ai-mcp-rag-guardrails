/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MessageResource } from '../../models/message-resource';

export interface GetConversationMessages$Params {

/**
 * UUID of the conversation
 */
  conversationId: string;
}

export function getConversationMessages(http: HttpClient, rootUrl: string, params: GetConversationMessages$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<MessageResource>>> {
  const rb = new RequestBuilder(rootUrl, getConversationMessages.PATH, 'get');
  if (params) {
    rb.path('conversationId', params.conversationId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<MessageResource>>;
    })
  );
}

getConversationMessages.PATH = '/api/conversations/{conversationId}/messages';
