/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ChatbotResource } from '../../models/chatbot-resource';

export interface GetChatbot$Params {
  id: string;
}

export function getChatbot(http: HttpClient, rootUrl: string, params: GetChatbot$Params, context?: HttpContext): Observable<StrictHttpResponse<ChatbotResource>> {
  const rb = new RequestBuilder(rootUrl, getChatbot.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ChatbotResource>;
    })
  );
}

getChatbot.PATH = '/api/chatbots/{id}';
