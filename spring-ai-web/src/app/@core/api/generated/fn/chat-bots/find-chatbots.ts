/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LazyLoadingEventRequest } from '../../models/lazy-loading-event-request';
import { PageChatbotResource } from '../../models/page-chatbot-resource';

export interface FindChatbots$Params {
  request: LazyLoadingEventRequest;
}

export function findChatbots(http: HttpClient, rootUrl: string, params: FindChatbots$Params, context?: HttpContext): Observable<StrictHttpResponse<PageChatbotResource>> {
  const rb = new RequestBuilder(rootUrl, findChatbots.PATH, 'get');
  if (params) {
    rb.query('request', params.request, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageChatbotResource>;
    })
  );
}

findChatbots.PATH = '/api/chatbots/paged';
