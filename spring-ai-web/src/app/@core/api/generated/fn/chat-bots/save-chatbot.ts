/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ChatbotResource } from '../../models/chatbot-resource';

export interface SaveChatbot$Params {
      body: ChatbotResource
}

export function saveChatbot(http: HttpClient, rootUrl: string, params: SaveChatbot$Params, context?: HttpContext): Observable<StrictHttpResponse<ChatbotResource>> {
  const rb = new RequestBuilder(rootUrl, saveChatbot.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ChatbotResource>;
    })
  );
}

saveChatbot.PATH = '/api/chatbots';
